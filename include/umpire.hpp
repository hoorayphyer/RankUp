#ifndef _TABLE_HPP_
#define _TABLE_HPP_

class Table {
private:
  // // visibility controls the level of chronicles each player can access.
  // std::array<int,4> _visibility;
  std::array<Card,8> _reserve;
public:
};

#endif

